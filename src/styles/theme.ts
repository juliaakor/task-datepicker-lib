import {
  borderWidth,
  size,
  spacing,
  colors,
  fontSize,
  fontFamily,
  fontWeight,
  lineHeight,
  borderRadius,
} from '@styles/variables';

export const defaultTheme = {
  colors,
  font: {
    family: fontFamily,
    height: lineHeight,
    size: fontSize,
    weight: fontWeight,
  },
  units: {
    borderRadius,
    borderWidth,
    size,
    spacing,
  },
};
