import './font.css';

import { size, width, colors, fontSize, fontFamily, fontWeight, lineHeight } from '@styles/variables';

export const defaultTheme = {
  colors,
  fontFamily,
  fontSize,
  fontWeight,
  lineHeight,
  size,
  width,
};
